bloodthirsty_battleend
OnBattleEnd
def bloodthirsty_battleend(self, battle_instance=0,**kwargs):
    if self.target.in_battle !=battle_instance:
        return #print("not battle self")
    self.Recover()
    if flag_debuglog>=5:print("end battle recover",self.name)
    #self.value=self.magnitude*(injured_proportion)*100
    #print("atkrate buff",self.value)
    #self.Enforce()